name: Deploy frontend
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run tests
        run: |
          docker build -t dockwall/vite-react-docker-test -f Dockerfile.dev .
          docker run -e CI=true dockwall/vite-react-docker-test npm run test -- run

      - name: Generate deployment package
        run: zip -r deploy.zip . -x '*.git*'

      - name: Copy deployment package
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_DOMAIN }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "deploy.zip"
          target: ${{ secrets.VPS_APP_PATH }}

      - name: Build project image and run a container
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_DOMAIN }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            cd ${{ secrets.VPS_APP_PATH }}
            unzip -o deploy.zip
            rm deploy.zip
            sudo docker stop ${{secrets.VPS_CONTAINER_NAME}}
            sudo docker rm ${{secrets.VPS_CONTAINER_NAME}}
            sudo docker rmi ${{secrets.VPS_IMAGE_NAME}}
            sudo docker build -t ${{secrets.VPS_IMAGE_NAME}} .
            sudo docker run -d -p 127.0.0.1:${{ secrets.VPS_APP_PORT }}:80 --name ${{secrets.VPS_CONTAINER_NAME}} ${{secrets.VPS_IMAGE_NAME}}:latest
